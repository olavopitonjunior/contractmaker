feat(propostas): envio pedido pelo corretor executa na hora

Teste em producao: o corretor pediu o envio, o Newton chamou send_proposal
"ok=true" — e nada saiu. `requireApproval` criou um ActionIntent PROPOSAL_SEND
`pending`, que so a tela do app aprova. Quem pediu estava no WhatsApp e nao tem
como aprovar; o pedido morreria em 24h no `expirePendingIntents`. Ele disse
"Confirmo" e depois "Esta aprovado, pode mandar", e continuou pendente — le-se
como "o agente nao faz".

O gate estava protegendo contra a acao errada. HITL existe pra decisao que o
humano ainda nao tomou; aqui o pedido dele E a decisao. Confirmar duas vezes so
adia.

`requireApproval` ganha `autoApprove`, e /send e /send-vendedor passam a usar.
A intent CONTINUA sendo criada e vira `executed` (ou `failed`, nunca `pending`,
que mentiria dizendo que alguem ainda vai aprovar): o rastro de quem pediu, com
que payload e com que resultado e o que torna a acao auditavel, e e o que faz o
replay com a mesma idempotencyKey devolver o resultado guardado em vez de gastar
ClickSign de novo. Some so o passo humano.

PROPOSAL_CANCEL e PROPOSAL_CONVERT seguem gateados — cancelar destroi envelope
em curso e converter cria negocio; nenhum dos dois e "o que o corretor acabou
de pedir".

Nas descricoes das tools sai o "SEMPRE confirma verbalmente" (era ele que
produzia a rodada extra de "Confirmo") e entra o aviso de conferir os dados
ANTES de chamar, ja que o envio e irreversivel e custa. `send_proposal` tambem
passa a dizer que o proposalId e o devolvido por create_proposal: no teste o
modelo mandou o id do ActionIntent.

Bug de teste corrigido de passagem: o beforeEach nao resetava
`actionIntent.update`, entao assercao sobre `mock.calls[0]` lia a chamada do
teste anterior.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01P6QKDjGGtHppeyJeNjwpAd
