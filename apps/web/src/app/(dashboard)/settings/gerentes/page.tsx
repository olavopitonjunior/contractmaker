import { ManagerSettingsClient } from "@/components/settings/ManagerSettingsClient";

export const metadata = {
  title: "Gerentes",
};

export default function ManagerSettingsPage() {
  return <ManagerSettingsClient />;
}
